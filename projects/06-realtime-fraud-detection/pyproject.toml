[project]
name = "realtime-fraud-detection"
version = "0.1.0"
description = "Deteccao de fraude em tempo real com Kinesis + Flink vs batch"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = [
    "boto3>=1.34",
    "polars>=1.0",
    "plotly>=5.20",
    "pyarrow>=15.0",
]
