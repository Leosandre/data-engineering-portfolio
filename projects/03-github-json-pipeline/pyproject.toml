[project]
name = "github-json-pipeline"
version = "0.1.0"
description = "Flatten de JSON aninhado da API do GitHub"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "polars>=1.0",
    "pyarrow>=15.0",
    "duckdb>=0.10",
    "plotly>=5.18",
    "pandas>=2.1",
]
